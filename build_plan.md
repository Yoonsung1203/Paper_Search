# 신규 논문 스크리닝 — Build Plan

> [PRD.md](./PRD.md) v0.2를 구현 계획으로 옮긴 문서입니다.
> PRD가 **무엇을 만드는가**를 정의한다면, 이 문서는 **어떤 순서로 어떻게 만드는가**를 정의합니다.

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v1.0 |
| 최종 수정일 | 2026-08-25 |
| 대상 PRD | PRD.md v0.2 |
| 저장소 | [Yoonsung1203/Paper_Search](https://github.com/Yoonsung1203/Paper_Search) |

---

## 1. 확정된 기술 결정

PRD §13(미해결 항목)에서 이번에 확정된 사항입니다.

| # | 항목 | 결정 | 근거 |
| --- | --- | --- | --- |
| 1 | 구현 언어 | **Python 3.11+** | 생물의학 API 생태계·데이터 처리·Anthropic SDK 지원이 가장 두텁다 |
| 2 | 결과 전달 / Human gate | **웹 UI** | 초록을 읽으며 선택하는 작업은 화면이 있어야 15분 목표를 지킬 수 있다 |
| 3 | LLM 모델 | **`claude-opus-5` 전 단계 단일 사용** | 선택 판단의 질이 KPI(1차 선택률 70%)를 직접 좌우한다 |

### 1.1 계획 수립을 위해 세운 가정 (이견 있으면 조정)

| 가정 | 내용 | 대안 |
| --- | --- | --- |
| A-1 | 웹 스택은 **FastAPI + Jinja2 + HTMX**로 하고 Node 빌드 체인을 두지 않는다 | 화면 요구가 커지면 M4 이후 React 분리 |
| A-2 | v1은 **로컬 단일 사용자** 실행(`uvicorn` 로컬 기동). 인증·배포 없음 | 팀 공유가 필요해지면 별도 마일스톤 |
| A-3 | 저장소는 **SQLite** 단일 파일 | 다중 사용자 전환 시 Postgres |
| A-4 | 라운드당 LLM 비용 상한 **$3** (PRD §13-5) | 실측 후 재조정 |

---

## 2. 시스템 아키텍처

### 2.1 데이터 흐름

```
 [사용자] ──키워드/기간/임계값──▶ ┌──────────────┐
                                  │  Web (FastAPI)│
                                  └───────┬───────┘
                                          │ 라운드 생성
                                          ▼
                            ┌─────────────────────────┐
                            │  Pipeline Orchestrator   │
                            └─────────────────────────┘
              ┌───────────────┬───────────┴───────┬──────────────┐
              ▼               ▼                   ▼              ▼
        ┌──────────┐   ┌──────────┐        ┌──────────┐   ┌──────────┐
        │  PubMed  │   │ bioRxiv  │        │ Crossref │   │ OpenAlex │
        │  (검색)  │   │(날짜수집)│        │ (DOI정규)│   │ (저널지표)│
        └────┬─────┘   └────┬─────┘        └────┬─────┘   └────┬─────┘
             └──────────────┴───────┬───────────┴──────────────┘
                                    ▼
                          ┌───────────────────┐
                          │ Dedupe (DOI 기준) │
                          └─────────┬─────────┘
                                    ▼
                     ┌──────────────────────────────┐
                     │ LLM 단계 (claude-opus-5)      │
                     │  F-03 관련도 점수화 (전건)     │
                     │  F-02 요약 · F-04 차별성(상위) │
                     └──────────────┬───────────────┘
                                    ▼
                          ┌───────────────────┐
                          │ 1차 리스트 (웹)   │◀── [사용자 선택: Human gate]
                          └─────────┬─────────┘
                                    ▼
                     ┌──────────────────────────────┐
                     │ F-06 선택 기준 추론 → 재랭킹  │
                     └──────────────┬───────────────┘
                                    ▼
                          ┌───────────────────┐
                          │   최종 리스트     │
                          └───────────────────┘
                                    │
                              SQLite에 라운드 전체 기록 (재현성)
```

### 2.2 디렉터리 구조

```
Paper_Search/
├── pyproject.toml
├── .env.example                  # ANTHROPIC_API_KEY, NCBI_API_KEY, CONTACT_EMAIL
├── PRD.md
├── build_plan.md
├── src/paper_search/
│   ├── config.py                 # 설정·환경변수 로딩
│   ├── models.py                 # Pydantic: Paper, Score, Summary, Round, Selection
│   ├── sources/                  # 외부 API 어댑터 (공통 Protocol)
│   │   ├── base.py
│   │   ├── pubmed.py             # F-01
│   │   ├── biorxiv.py            # F-01 (프리프린트)
│   │   ├── crossref.py           # DOI 정규화·메타 보강
│   │   └── openalex.py           # F-05 저널 지표
│   ├── core/
│   │   ├── ratelimit.py          # 소스별 토큰버킷 + 지수 백오프
│   │   ├── cache.py              # HTTP 응답 디스크 캐시
│   │   ├── dedupe.py             # DOI/제목 정규화 기반 중복 제거
│   │   └── pipeline.py           # 라운드 오케스트레이션
│   ├── llm/
│   │   ├── client.py             # Anthropic 클라이언트 + 동시성 제어 + 비용 집계
│   │   ├── schemas.py            # structured output 스키마
│   │   ├── score.py              # F-03
│   │   ├── summarize.py          # F-02
│   │   ├── novelty.py            # F-04
│   │   └── compare.py            # F-06
│   ├── store/
│   │   ├── db.py                 # SQLite 연결·마이그레이션
│   │   └── repository.py
│   └── web/
│       ├── app.py                # FastAPI 진입점
│       ├── routes.py
│       ├── templates/            # Jinja2
│       └── static/
└── tests/
    ├── fixtures/                 # 외부 API 응답 고정본
    └── golden/                   # 사용자 라벨링 세트 (§7.3)
```

### 2.3 데이터 모델 (SQLite)

| 테이블 | 주요 컬럼 | 목적 |
| --- | --- | --- |
| `round` | `id`, `created_at`, `keywords`, `date_from`, `date_to`, `impact_threshold`, `status`, `cost_usd` | 라운드 입력·상태·비용 (재현성, PRD §10) |
| `paper` | `doi` (PK), `title`, `authors`, `journal`, `issn`, `published_at`, `abstract`, `url`, `source`, `is_preprint` | 논문 원본 메타 (라운드 간 재사용) |
| `journal_metric` | `issn` (PK), `metric_name`, `metric_value`, `source`, `year`, `fetched_at` | 임팩트 대용 지표 캐시 (F-05) |
| `round_paper` | `round_id`, `doi`, `relevance_score`, `evidence`, `summary`, `summary_basis`, `novelty`, `rank` | 라운드별 LLM 산출물 |
| `selection` | `round_id`, `doi`, `selected` (bool), `selected_at` | Human gate 결과 (F-06 입력, KPI 측정) |
| `criteria` | `round_id`, `text`, `origin` (`inferred`/`user_edited`), `active` | F-06이 추론한 선택 기준 |
| `llm_call` | `id`, `round_id`, `stage`, `model`, `input_tokens`, `output_tokens`, `cost_usd` | 비용 상한(A-4) 집행·감사 |

---

## 3. LLM 호출 설계

### 3.1 단계별 정의

| 단계 | PRD | 입력 | 출력 (structured) | 호출 수 / 라운드 |
| --- | --- | --- | --- | --- |
| S1. 관련도 점수화 | F-03 | 제목 + 초록 + 사용자 키워드 (+ 활성 criteria) | `{score: 0-100, evidence: [str], reason: str}` | 후보 전건 (~150–250) |
| S2. 요약 | F-02 | 초록 또는 전문 | `{summary: [str×3~5], basis: "abstract"|"fulltext"}` | 상위 N건 (기본 30) |
| S3. 차별성 검증 | F-04 | 요약 + 저널 정보 | `{claim: str, category: enum, confidence: enum}` | S2와 동일 건 (1회 호출로 병합 가능) |
| S4. 그룹 비교 | F-06 | 선택군 A / 비선택군 B 메타 | `{criteria: [{text, supporting_dois}]}` | 라운드당 1회 |

- **모든 단계에 structured output을 강제**한다. Pydantic 모델 + `client.messages.parse(..., output_format=Model)`을 사용해 `response.parsed_output`을 검증된 객체로 받는다. 파싱 실패를 애플리케이션 코드에서 다루지 않기 위함이다.
- **적응형 사고(adaptive thinking)** 를 기본값으로 둔다: `thinking={"type": "adaptive"}`. S1처럼 대량·단순한 판단은 `output_config={"effort": "low"}`로 낮추고, S3·S4는 `high`를 쓴다.
- **프롬프트 캐싱**: 시스템 프롬프트(판단 기준·출력 규칙)를 고정 접두부로 두고 `cache_control`을 건다. 논문별 가변 내용은 반드시 그 뒤에 배치한다. `usage.cache_read_input_tokens`가 0이면 캐시가 깨진 것이므로 회귀 테스트에서 확인한다.
- **S3는 S2와 한 번의 호출로 합친다.** 같은 본문을 두 번 보내면 입력 토큰이 2배가 된다.

### 3.2 동시성과 Batch API 사용 시점 — 주의할 충돌

Batch API는 토큰 비용이 50%지만 **완료까지 최대 24시간**이 걸릴 수 있습니다. PRD §10의 "1라운드 1시간 이내" 목표와 충돌할 수 있으므로 다음과 같이 나눕니다.

| 시점 | 방식 | 이유 |
| --- | --- | --- |
| M2까지 (기본) | 실시간 호출 + `asyncio` 동시성 8 제한 | 응답 시간 예측 가능, PRD 성능 목표 준수 |
| M3 이후 (옵션) | S1 단계만 Batch API로 전환 가능한 `--batch` 플래그 | 비용이 A-4 상한에 근접할 때만 사용. "예약 실행" UX와 함께 도입 |

### 3.3 비용 추정 (라운드 1회, 개략치)

`claude-opus-5` 실시간 기준 입력 $5 / 출력 $25 per 1M tokens.

| 단계 | 건수 | 입력 tok/건 | 출력 tok/건 | 소계 |
| --- | --- | --- | --- | --- |
| S1 점수화 | 200 | ~800 | ~150 | 입력 $0.80 + 출력 $0.75 = **$1.55** |
| S2+S3 요약·차별성 | 30 | ~6,000 | ~600 | 입력 $0.90 + 출력 $0.45 = **$1.35** |
| S4 그룹 비교 | 1 | ~15,000 | ~1,500 | **$0.11** |
| | | | **합계** | **≈ $3.0 / 라운드** |

> 프롬프트 캐싱 적용 시 S1의 입력 비용이 크게 줄어들 것으로 보이나, **위 수치는 캐시 미적용 상한선**입니다.
> A-4 상한($3)에 딱 걸리므로, **M2 종료 시 실측하여 상한 또는 S2 건수(기본 30)를 조정**해야 합니다. 이것이 M2의 명시적 산출물입니다.

### 3.4 비용 가드레일
- 라운드 시작 시 예상 비용을 추산해 상한 초과가 예상되면 **사용자에게 확인을 받는다**.
- `llm_call` 테이블에 누적 비용을 기록하고, 상한 도달 시 파이프라인을 중단한 뒤 **부분 결과를 반환**한다(조용히 잘라내지 않는다).

---

## 4. 외부 API 통합 설계

| 소스 | 호출 방식 | Rate limit 대응 | 캐싱 |
| --- | --- | --- | --- |
| PubMed E-utilities | `esearch` → `esummary`/`efetch` 배치(200건 단위) | API 키 없으면 3 req/s, 키 발급 시 10 req/s → 토큰버킷으로 강제 | PMID 단위 디스크 캐시 |
| bioRxiv / medRxiv | `details/{server}/{interval}` 로 날짜 구간 수집 후 **자체 필터** | 페이지네이션 100건 단위, 요청 간 간격 확보 | 날짜 구간 단위 캐시 |
| Crossref | `works/{doi}` | polite pool — User-Agent에 연락처 명시 | DOI 단위 영구 캐시 |
| OpenAlex | `sources/issn:{issn}` | `mailto` 파라미터로 polite pool | ISSN 단위, TTL 30일 |

**공통 규칙**
- 모든 어댑터는 `sources/base.py`의 동일 Protocol을 구현하고 `list[Paper]`를 반환한다 → 소스 추가가 파이프라인 변경 없이 가능해진다.
- 429/5xx는 지수 백오프(2s → 4s → 8s → 16s, 최대 4회)로 재시도한다.
- **한 소스가 완전히 실패해도 라운드는 계속 진행**하고, 결과 화면에 `bioRxiv 수집 실패` 배너를 띄운다.

### 4.1 bioRxiv 키워드 검색 부재에 대한 구체적 대응 (PRD §6.2-1)
1. 사용자 키워드 → 관련 bioRxiv 카테고리 매핑 (초기엔 수동 매핑 테이블, 30개 내외)
2. 해당 카테고리 × 기간으로 전량 수집 (7일 기준 카테고리당 수백 건)
3. 제목/초록에 대한 값싼 사전 필터(키워드 문자열 + 동의어)로 후보를 200건 이하로 축소
4. 그 다음에야 S1(LLM 점수화)에 태운다 — **전량을 LLM에 넣지 않는다**

---

## 5. 마일스톤별 작업 분해

각 작업은 별도 브랜치 → PR → `main` merge 단위입니다(§8).

### M0 — 프로젝트 스캐폴딩
| ID | 작업 | 완료 기준 | 의존 |
| --- | --- | --- | --- |
| T0-1 | `pyproject.toml`, 패키지 골격, `.env.example` | `pip install -e .` 후 `python -c "import paper_search"` 성공 | — |
| T0-2 | `config.py` — 환경변수·설정 로딩 | 키 누락 시 명확한 에러 메시지 | T0-1 |
| T0-3 | `models.py` — Pydantic 도메인 모델 | Paper/Round/Score 등 정의 및 단위 테스트 | T0-1 |
| T0-4 | `store/db.py` — SQLite 스키마 및 마이그레이션 | §2.3 전 테이블 생성·롤백 | T0-3 |
| T0-5 | CI (GitHub Actions): ruff + mypy + pytest | PR에서 자동 실행, 네트워크 테스트는 스킵 | T0-1 |

### M1 — 검색 파이프라인 (PRD F-01)
| ID | 작업 | 완료 기준 | 의존 |
| --- | --- | --- | --- |
| T1-1 | `core/ratelimit.py`, `core/cache.py` | 토큰버킷·백오프 단위 테스트 통과 | T0-2 |
| T1-2 | `sources/pubmed.py` | 키워드+기간으로 `list[Paper]` 반환, 고정본 기반 테스트 | T1-1 |
| T1-3 | `sources/biorxiv.py` + 카테고리 매핑 + 사전 필터 (§4.1) | 7일 구간 수집 후 200건 이하로 축소 | T1-1 |
| T1-4 | `sources/crossref.py` — DOI 정규화·메타 보강 | DOI 없는 레코드에 DOI 보강 | T1-1 |
| T1-5 | `core/dedupe.py` | 동일 논문의 PubMed/bioRxiv 중복이 1건으로 병합 | T1-2, T1-3 |
| T1-6 | 연구자 이름 검색 경로 | 저자명 입력 시 해당 저자 논문 + 연구분야 논문 반환 | T1-2 |
| **M1 게이트** | | **키워드+기간 입력 → 중복 제거된 후보 리스트가 DB에 저장된다** | |

### M2 — 스크리닝 루프 + 웹 UI (PRD F-02, F-03, Human gate)
| ID | 작업 | 완료 기준 | 의존 |
| --- | --- | --- | --- |
| T2-1 | `llm/client.py` — 클라이언트, 동시성 제한, 비용 집계 | `llm_call` 기록 및 상한 도달 시 중단 | T0-4 |
| T2-2 | `llm/score.py` (S1) + structured output 스키마 | 점수와 근거가 항상 스키마에 맞게 반환 | T2-1 |
| T2-3 | `llm/summarize.py` (S2, S3 병합) | `basis` 필드로 초록/전문 근거 범위 구분 | T2-1 |
| T2-4 | `core/pipeline.py` — 라운드 오케스트레이션 + 상태 머신 | 비동기 실행, 상태가 `round.status`에 반영 | T1-5, T2-2 |
| T2-5 | 웹: 키워드 입력 화면 | 기간 기본 7일, 임계값 입력 | T0-4 |
| T2-6 | 웹: 진행 상태 화면 (HTMX 폴링) | 단계별 진행률과 예상 비용 표시 | T2-4 |
| T2-7 | 웹: 1차 리스트 + 선택 (Human gate) | 점수·근거·요약·`preprint` 배지 표시, 선택이 `selection`에 저장 | T2-4 |
| T2-8 | 예외 흐름 (PRD §7) | 5건 미만 / 200건 초과 / 미선택 케이스 처리 | T2-7 |
| T2-9 | **비용 실측 및 A-4 상한 재조정** | 실제 라운드 3회 실측치를 이 문서에 반영 | T2-4 |
| **M2 게이트** | | **웹에서 키워드 입력 → 1차 리스트 선택 → 최종 리스트까지 완주된다 (제품 최소 형태)** | |

### M3 — 판단 근거 강화 (PRD F-04, F-05)
| ID | 작업 | 완료 기준 | 의존 |
| --- | --- | --- | --- |
| T3-1 | `sources/openalex.py` + `journal_metric` 캐시 | ISSN → 지표값·출처·기준연도 반환 | T1-1 |
| T3-2 | 임팩트 임계값 필터 + 프리프린트 우회 정책 | 프리프린트가 필터로 탈락하지 않음, `미확인` 저널도 유지 | T3-1 |
| T3-3 | `llm/novelty.py` (F-04) 결과를 UI에 노출 | 근거 미확인 시 `판단 보류` 표시 | T2-3 |
| T3-4 | UI 지표 표기 규칙 적용 | "IF" 단독 표기 금지, 지표명·출처·연도 병기 | T3-1 |
| T3-5 | (옵션) S1 Batch API 경로 | `--batch` 사용 시 비용 50% 감소 확인 | T2-2 |
| **M3 게이트** | | **임팩트 지표와 차별성 근거가 리스트에 표시되고, 임계값 필터가 동작한다** | |

### M4 — 학습 루프 (PRD F-06)
| ID | 작업 | 완료 기준 | 의존 |
| --- | --- | --- | --- |
| T4-1 | `llm/compare.py` (S4) | 선택군/비선택군에서 기준 3~5개 추론 | T2-7 |
| T4-2 | 추론 기준의 재랭킹 반영 | 활성 criteria가 S1 프롬프트에 주입됨 | T4-1 |
| T4-3 | 기준 편집 UI | 사용자가 기준 수정·삭제·비활성화 가능 | T4-1 |
| T4-4 | 라운드 간 이력 유지 (PRD §13-4) | 이전 라운드의 활성 기준을 다음 라운드가 승계 | T4-2 |
| T4-5 | KPI 대시보드 | 선택률·최종 건수·소요시간을 라운드별로 확인 | T2-7 |
| **M4 게이트** | | **2라운드 이상 실행 시 선택률이 1라운드 대비 개선되는 것이 KPI 대시보드에서 확인된다** | |

---

## 6. 마일스톤 ↔ PRD 대응표

| PRD 기능 | 우선순위 | 구현 마일스톤 |
| --- | --- | --- |
| F-01 논문 검색 | Must | M1 |
| F-02 핵심 요약 | Should | M2 (T2-3) |
| F-03 관련도 점수화 | Should | M2 (T2-2) |
| F-04 차별성 검증 | Should | M3 (T3-3) |
| F-05 임팩트 확인 | Nice to Have | M3 (T3-1~4) |
| F-06 그룹 비교 | Nice to Have | M4 |

> **M2까지가 제품의 최소 형태(Walking Skeleton)** 입니다. M2 게이트를 통과하지 못한 상태에서 M3·M4에 착수하지 않습니다.

---

## 7. 검증 전략

### 7.1 계층별 테스트
| 계층 | 방식 | 비고 |
| --- | --- | --- |
| 순수 로직 (dedupe, ratelimit, 필터) | 일반 단위 테스트 | 네트워크·LLM 없음 |
| 외부 API 어댑터 | `tests/fixtures/`의 **고정 응답본**으로 테스트 | 실제 호출은 `-m network` 마킹으로 분리, CI 기본 제외 |
| LLM 단계 | 스키마 검증은 스텁으로, 판단 품질은 §7.3 골든셋으로 | 매 테스트마다 API를 호출하지 않는다 |
| 파이프라인 | 어댑터·LLM 모두 스텁으로 대체한 end-to-end | 상태 전이와 부분 실패 경로 확인 |

### 7.2 반드시 넣을 회귀 테스트
- 동일 논문이 PubMed와 bioRxiv에 모두 있을 때 1건으로 병합되는가
- 한 소스가 실패해도 라운드가 완주하는가
- 비용 상한 도달 시 **부분 결과가 반환되고 조용히 잘리지 않는가**
- 프롬프트 캐시가 실제로 적중하는가 (`usage.cache_read_input_tokens > 0`)
- 프리프린트가 임팩트 필터로 탈락하지 않는가

### 7.3 골든셋 — KPI 개선의 전제
LLM 프롬프트를 바꿀 때마다 판단 품질이 좋아졌는지 나빠졌는지 알 방법이 필요합니다.

- **M2 착수 전에** 사용자가 논문 30~50편에 대해 "관심 있음/없음"을 직접 라벨링해 `tests/golden/`에 둔다.
- 프롬프트나 모델 설정을 바꿀 때마다 이 세트로 **선택률(정밀도)과 놓침률**을 측정한다.
- 이것 없이는 PRD §9의 "1차 선택률 70%"를 개선할 수단이 없다. **T2-2의 선행 조건으로 취급한다.**

---

## 8. 개발 워크플로우

`main`이 항상 최신 상태를 유지하도록 다음 절차를 지킵니다.

1. `main`에서 작업 브랜치를 딴다 — 이름은 `feat/`, `fix/`, `docs/`, `chore/` 접두어 + 작업 ID (예: `feat/t1-2-pubmed-source`)
2. 작업 단위는 **작업 ID 1개**를 원칙으로 한다 (PR이 리뷰 가능한 크기를 넘지 않도록)
3. 푸시 전 로컬에서 `ruff` + `mypy` + `pytest`를 통과시킨다
4. PR 본문에 **대상 작업 ID·완료 기준 충족 여부·검증 방법**을 적는다
5. CI 통과 후 `main`으로 merge, 작업 브랜치는 삭제한다
6. 마일스톤 게이트를 통과하면 이 문서의 해당 체크리스트를 갱신한다

---

## 9. 리스크 (구축 관점)

| 리스크 | 신호 | 대응 |
| --- | --- | --- |
| 라운드당 비용이 상한을 넘음 | T2-9 실측치가 $3 초과 | S2 대상 건수 축소, S1에 값싼 사전 필터 강화, Batch API 전환 |
| bioRxiv 수집량 폭증 | 카테고리당 일 수백 건 이상 | 사전 필터를 LLM 앞단에서 더 공격적으로, 카테고리 매핑 축소 |
| Batch API 지연이 성능 목표와 충돌 | 라운드가 1시간을 넘김 | 기본을 실시간 호출로 유지 (§3.2), Batch는 옵션으로만 |
| 요약의 사실 왜곡 | 골든셋 검증 중 원문에 없는 주장 발견 | 초록 원문 인용을 출력 스키마에 필수 필드로 포함 |
| 임팩트 대용 지표가 기대와 다름 | 사용자가 "IF 10점"과 결과가 안 맞는다고 느낌 | T3-4의 표기 규칙 준수, 지표별 대략적 환산 안내 제공 |
| 골든셋 라벨링이 지연됨 | M2 착수 시점에 라벨 없음 | M2 게이트를 "골든셋 확보"까지 포함해 정의 (§7.3) |

---

## 10. 완료 정의 (Definition of Done)

작업 단위 DoD:
- [ ] 완료 기준을 만족하는 테스트가 있고 통과한다
- [ ] `ruff` + `mypy` 통과
- [ ] 외부 API/LLM 호출부는 고정본 또는 스텁으로 테스트 가능하다
- [ ] 실패 경로(네트워크 오류, 빈 결과, 비용 초과)가 처리되어 있다
- [ ] PR이 merge되어 `main`이 최신 상태다

마일스톤 DoD:
- [ ] 게이트 조건을 실제 실행으로 확인했다 (스크린샷 또는 로그)
- [ ] PRD의 해당 수용 기준 체크박스가 채워졌다
- [ ] 이 문서의 가정·추정치가 실측으로 갱신되었다

---

## 11. 다음 액션

1. **M0 착수** — T0-1 ~ T0-5 (프로젝트 골격 + CI)
2. **골든셋 라벨링 요청** — 사용자가 관심/비관심 논문 30~50편 라벨링 (§7.3, M2 선행 조건)
3. **NCBI API 키 발급** — rate limit 3 req/s → 10 req/s (§4)
4. **일정 확정** — PRD §12의 마일스톤 날짜는 개발 가용 시간 확인 후 기입

---

## 변경 이력

| 버전 | 일자 | 내용 |
| --- | --- | --- |
| v1.0 | 2026-08-25 | 최초 작성. PRD v0.2 기반, 스택·전달수단·모델 결정 반영 |
