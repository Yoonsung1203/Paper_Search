# 신규 논문 스크리닝 (Paper Search) — PRD

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v0.3 (draft) |
| 최종 수정일 | 2026-08-25 |
| 상태 | 초안 — 기능 정의(§5) 확정, 일정(§12)·미해결 항목(§13) 논의 필요 |
| 관련 저장소 | [Yoonsung1203/Paper_Search](https://github.com/Yoonsung1203/Paper_Search) |
| 구현 계획 | [build_plan.md](./build_plan.md) |

---

## 0. 한 줄 요약 (TL;DR)

연구자가 **키워드 또는 관심 연구자 이름**을 입력하면, 최근 1주일 이내 공개된 논문 중
**저널 임팩트 수준 기준을 만족하고 관심사에 부합하는 논문 리스트**를 자동으로 만들어 준다.
사용자는 1차 리스트에서 관심 논문을 직접 고르고(**Human gate**), 시스템은 그 선택을 학습해
**정제된 최종 리스트**를 다시 제시한다.

---

## 1. 배경 및 문제 정의

| # | 관찰 | 그래서 생기는 문제 |
| --- | --- | --- |
| 1 | 매일 수천 건의 신규 논문·프리프린트가 공개된다 | 수동 확인은 현실적으로 불가능 |
| 2 | 연구자는 본업(실험·분석·집필)에 시간을 쓴다 | 검색에 쓸 수 있는 시간이 주당 1시간 미만 |
| 3 | High-impact 저널 논문은 각각 뚜렷한 차별점(새 방법론, 대규모 데이터, 새 이론 등)을 갖는다 | 이런 논문을 놓치면 분야의 방향 전환을 놓친다 |
| 4 | 기존 알림 서비스(PubMed alert 등)는 키워드 문자열 매칭 위주다 | 재현율은 높지만 정밀도가 낮아 결국 사람이 다 읽어야 한다 |

> **핵심 문제**: 재현율(빠짐없이 찾기)과 정밀도(내 관심사만 남기기)를 동시에 만족하는
> 주간 스크리닝 루틴이 없다.

---

## 2. 목표 / 비목표

### 2.1 목표 (In scope)
- 주 1회, **15분 이내의 사용자 개입**으로 관심 논문 최종 리스트를 확보한다.
- 저널 임팩트 수준을 **필터 조건으로 명시적으로 지정**할 수 있게 한다.
- 사용자의 선택/비선택을 **다음 라운드 랭킹에 반영**한다.

### 2.2 비목표 (Out of scope, v1 기준)
- 논문 전문(全文) 번역 및 상세 리뷰 작성
- 참고문헌 그래프 분석, 인용 네트워크 시각화
- 다중 사용자 협업 / 팀 공유 기능
- 논문 PDF 저장·관리(레퍼런스 매니저 기능) — Zotero/Mendeley 대체 아님
- 유료 데이터베이스(Scopus, Web of Science, JCR) 라이선스 구매

---

## 3. 사용자 및 페르소나

### P1. 연구 업무에 바쁜 연구자 (Primary)
- **상황**: 실험·분석·논문 작성으로 주중 대부분의 시간이 채워져 있다.
- **니즈**: 트렌드는 따라가야 하지만, 검색 자체에 시간을 쓸 수 없다.
- **성공의 정의**: "주 1회 15분만 투자하면 놓친 논문이 없다"는 확신.
- **실패 시 행동**: 리스트에 관련 없는 논문이 절반 이상이면 다음 주부터 열어보지 않는다.

### P2. 분야 진입 초기 연구자 (Secondary, v2 이후)
- 키워드를 스스로 잘 정의하지 못한다 → **키워드 제안 기능**이 필요 (§13 미해결 항목).

---

## 4. 용어 정의

| 용어 | 정의 |
| --- | --- |
| **IF (Impact Factor)** | Clarivate JCR이 매년 발표하는 저널 지표. **유료·독점**이므로 본 프로젝트에서는 §6의 대체 지표를 사용한다. |
| **임팩트 지표(대용)** | OpenAlex `2yr_mean_citedness`, Scimago SJR, CiteScore 등 무료로 취득 가능한 저널 수준 지표. |
| **Human gate** | 1차 리스트에서 사용자가 관심 논문을 직접 선택하는 단계. 자동화 금지 구간. |
| **관련도 점수** | 논문 내용과 사용자 키워드 간 부합도를 0–100으로 정규화한 값. |
| **라운드** | `검색 → 1차 리스트 → 사용자 선택 → 재랭킹 → 최종 리스트`의 1회 실행 단위. |

---

## 5. 기능 요구사항

각 기능은 `입력 → 출력 → 수용 기준(Acceptance Criteria)` 형태로 정의한다.

### F-01. 논문 검색 · **Must**
- **입력**: 키워드 목록(AND/OR), 연구자 이름(선택), 발행일 범위(기본: 최근 7일), 임팩트 임계값
- **출력**: 후보 논문 목록 (제목, 저자, 저널, 발행일, DOI, 초록, 원문 링크)
- **수용 기준**
  - [ ] 기본값으로 "최근 7일"이 적용되고, 사용자가 임의 구간(예: 2026-08-01 ~ 2026-08-25)으로 변경할 수 있다.
  - [ ] 연구자 이름 입력 시, 해당 연구자의 신규 논문 **및** 그 연구자의 주 연구분야에 해당하는 논문을 함께 반환한다.
  - [ ] 동일 논문이 여러 소스(PubMed·bioRxiv 등)에서 잡히면 DOI 기준으로 **중복 제거**한다.

### F-02. 논문 핵심 요약 · **Should**
- **입력**: 초록 + (가능한 경우) 전문
- **출력**: 3~5줄 한국어 요약
- **수용 기준**
  - [ ] 전문 접근이 불가한 경우(비-OA 논문) 초록 기반으로 요약하고, **요약 근거 범위를 명시**한다(`초록 기반` / `전문 기반`).
  - [ ] 요약에 원문에 없는 수치·주장이 포함되지 않는다.

### F-03. 관련도 점수화 (키워드 비교) · **Should**
- **입력**: 논문 내용, 사용자 키워드
- **출력**: 0–100 관련도 점수 + 근거 문장 1~2개
- **수용 기준**
  - [ ] 점수는 리스트 정렬 기준으로 사용된다.
  - [ ] 왜 그 점수인지 사용자가 확인할 수 있도록 근거를 함께 제시한다.

### F-04. 차별성 검증 · **Should**
- **입력**: 논문 요약, 저널 정보
- **출력**: "이 논문이 왜 이 수준의 저널에 실렸는가"에 대한 1~2문장 설명 (예: 새 방법론 / 대규모 코호트 / 새 이론 / 임상적 파급력)
- **수용 기준**
  - [ ] 근거가 논문 본문에서 확인되지 않으면 `판단 보류`로 표시한다. 추측을 단정적으로 쓰지 않는다.

### F-05. 저널 임팩트 수준 확인 · **Nice to Have**
- **입력**: 저널명 / ISSN
- **출력**: 대용 임팩트 지표 값 + 출처 + 기준연도
- **수용 기준**
  - [ ] 프리프린트(bioRxiv/medRxiv)는 저널 지표가 없으므로 `preprint`로 별도 분류하며, 임팩트 필터로 탈락시키지 않는다(§6.3 참고).
  - [ ] 지표를 찾지 못한 저널은 `미확인`으로 표시하되 리스트에서 제외하지 않는다.

### F-06. 논문 그룹 비교 (선택 기준 추론) · **Nice to Have**
- **입력**: 사용자가 선택한 논문 그룹 A, 선택하지 않은 그룹 B
- **출력**: 두 그룹을 가르는 정성적 기준 3~5개 (예: "in vivo 검증 포함 여부", "인간 데이터 여부")
- **수용 기준**
  - [ ] 추론된 기준이 다음 라운드 재랭킹에 실제로 반영된다.
  - [ ] 사용자가 추론된 기준을 직접 수정·삭제할 수 있다.

---

## 6. 데이터 소스 및 제약

### 6.1 소스별 역할

| 소스 | 역할 | 키워드 검색 | 전문 제공 | 비고 |
| --- | --- | --- | --- | --- |
| PubMed / PMC E-utilities | 피어리뷰 논문 주 검색 | O | PMC OA subset만 | API 키 없으면 3 req/s, 키 발급 시 10 req/s |
| bioRxiv / medRxiv API | 프리프린트 수집 | **X (날짜·카테고리 기반)** | O (PDF) | 키워드 필터는 수집 후 자체 처리 필요 |
| Crossref | DOI 정규화, 메타데이터 보강 | O | X | polite pool 사용 시 연락처 헤더 권장 |
| OpenAlex | 저널 수준 지표, 저자 식별 | O | X | `2yr_mean_citedness`를 IF 대용으로 사용 |

### 6.2 반드시 인지해야 할 제약
1. **bioRxiv API는 키워드 검색을 지원하지 않는다.** 날짜/카테고리로 수집한 뒤 자체 필터링해야 하므로, 프리프린트 경로는 "수집량이 크고 필터가 무거운" 구조가 된다.
2. **JCR Impact Factor는 무료 API로 얻을 수 없다.** §4에 정의한 대용 지표를 쓰되, UI에서 "IF"라고 표기하지 말고 **지표명과 출처를 그대로 노출**한다.
3. **비-OA 논문은 전문을 얻을 수 없다.** F-02/F-04의 품질이 논문마다 달라지므로, 근거 범위를 항상 표시한다.

### 6.3 프리프린트 정책 (결정 필요 → §13)
현재 안: 프리프린트는 임팩트 필터를 우회시키되 리스트에서 `preprint` 배지로 구분 표시한다.

---

## 7. 사용자 시나리오 (User Flow)

```
[1] 키워드 / 연구자 입력          ← 사용자
        │  (+ 기간, 임팩트 임계값)
        ▼
[2] 검색 · 중복 제거 · 요약 · 점수화   ← 시스템 (F-01~F-05)
        │
        ▼
[3] 1차 리스트 확인 및 선택        ← 사용자 (Human gate)
        │  선택군 A / 비선택군 B
        ▼
[4] 선택 기준 추론 및 재랭킹        ← 시스템 (F-06)
        │
        ▼
[5] 최종 리스트 (15건 이상 목표)
```

**화면 흐름**: `키워드 입력` → `1차 리스트(선택)` → `최종 리스트`

**예외 흐름**
- 2단계 결과가 5건 미만 → 사용자에게 **키워드 완화 또는 기간 확장**을 제안한다.
- 2단계 결과가 200건 초과 → 임팩트 임계값 상향 또는 키워드 추가를 제안한다.
- 3단계에서 사용자가 아무것도 선택하지 않음 → F-06을 건너뛰고 관련도 점수 순 리스트를 그대로 제시한다.

---

## 8. 우선순위 (MoSCoW)

| 기능 | 우선순위 | 근거 |
| --- | --- | --- |
| F-01 논문 검색 | **Must** | 없으면 제품이 성립하지 않음 |
| F-02 핵심 요약 | Should | 리스트 훑는 시간을 좌우 |
| F-03 관련도 점수화 | Should | 정밀도(핵심 KPI)에 직접 기여 |
| F-04 차별성 검증 | Should | 선택 판단의 근거 제공 |
| F-05 임팩트 확인 | Nice to Have | 대용 지표 신뢰도 검증 후 승격 검토 |
| F-06 그룹 비교 | Nice to Have | 2라운드 이상 사용 시 가치 발생 |

---

## 9. 성공 지표 (KPI)

| 지표 | 정의 | 목표 | 측정 방법 |
| --- | --- | --- | --- |
| **1차 선택률 (정밀도)** | 1차 리스트 중 사용자가 선택한 논문 비율 | ≥ 70% | 라운드별 자동 기록 |
| **최종 리스트 규모** | 최종 리스트 논문 수 | ≥ 15건 / 주 | 라운드별 자동 기록 |
| **사용자 소요 시간** | 키워드 입력 ~ 최종 리스트 확인까지 사용자가 실제로 쓴 시간 | ≤ 15분 | 세션 타임스탬프 |
| **놓침률 (재현율 대용)** | 사후에 사용자가 다른 경로로 발견한 "리스트에 없었어야 할 이유가 없는" 논문 수 | ≤ 1건 / 월 | 사용자 수동 보고 |

> **주의**: 1차 선택률 70%는 상당히 높은 목표다. 초기 2~3라운드 실측 후 재보정한다.
> 선택률만 최적화하면 리스트를 짧게 만드는 방향으로 왜곡되므로, **반드시 최종 리스트 규모와 함께** 본다.

---

## 10. 비기능 요구사항

| 항목 | 요구사항 |
| --- | --- |
| **성능** | 1라운드 전체(사용자 대기 포함) 1시간 이내. 내부 목표: 자동 파이프라인 ≤ 10분, 사용자 개입 ≤ 15분 |
| **API 준수** | 각 소스의 rate limit 준수, User-Agent에 연락처 명시, 429 응답 시 지수 백오프 재시도 |
| **재현성** | 라운드별 입력(키워드·기간·임계값)과 결과를 저장하여 동일 조건 재실행 가능 |
| **투명성** | 모든 요약·점수에 출처 링크(DOI)와 근거 범위를 함께 표시 |
| **비용** | LLM 호출 비용을 라운드당 상한으로 관리 (상한값 §13에서 결정) |

---

## 11. 리스크 및 대응

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| API 부하 / rate limit 초과 | 검색 실패 | 요청 스로틀링 + 지수 백오프 재시도 + 부분 결과 반환 |
| 검색 결과 수 저조 | 리스트가 비어 사용자 이탈 | 키워드 완화·기간 확장 제안, 프리프린트 소스 자동 포함 |
| 요약의 사실 왜곡(hallucination) | 잘못된 논문 선택 → 신뢰 상실 | 초록 원문 인용 병기, 근거 없는 항목은 `판단 보류` 처리 |
| IF 대용 지표와 실제 IF의 괴리 | 필터가 기대와 다르게 동작 | UI에 지표명·출처·기준연도 명시, "IF" 단독 표기 금지 |
| bioRxiv 키워드 검색 미지원 | 프리프린트 재현율 저하 | 카테고리 기반 수집 후 자체 필터, 처리량 상한 설정 |
| 사용자 키워드가 모호함 | 정밀도 저하 | 라운드 종료 후 F-06 결과를 바탕으로 키워드 개선안 제시 |

---

## 12. 일정 및 릴리즈 계획

| 마일스톤 | 범위 | 완료 기준 |
| --- | --- | --- |
| **M1 — 검색 파이프라인** | F-01 | 키워드·기간으로 중복 제거된 후보 리스트 생성 |
| **M2 — 스크리닝 루프** | F-02, F-03 + Human gate | 1차 리스트에서 선택 후 최종 리스트 산출 |
| **M3 — 판단 근거 강화** | F-04, F-05 | 차별성 설명 및 임팩트 지표 표시 |
| **M4 — 학습 루프** | F-06 | 선택 기준 추론이 다음 라운드에 반영 |

> 각 마일스톤의 날짜는 §13의 개발 리소스 확정 후 기입한다.

---

## 13. 미해결 항목 (Open Questions)

| # | 질문 | 상태 | 결정 필요 시점 |
| --- | --- | --- | --- |
| 1 | 프리프린트를 기본 포함할 것인가, 옵션으로 둘 것인가? (§6.3) | 미정 | M1 |
| 2 | 임팩트 대용 지표로 무엇을 1순위로 쓸 것인가? (OpenAlex vs SJR) | 미정 | M3 |
| 3 | 결과 전달 수단은? | **결정 — 웹 UI** ([build_plan.md](./build_plan.md) §1) | — |
| 4 | 사용자 선택 이력을 라운드 간 영구 저장할 것인가? | 미정 | M4 |
| 5 | 라운드당 LLM 비용 상한은? | 잠정 $3 / 라운드 (build_plan §1 A-4) — M2에서 실측 후 확정 | M2 |
| 6 | 구현 언어·LLM 모델 | **결정 — Python 3.11+ / `claude-opus-5`** (build_plan §1) | — |

---

## 14. 관련 문서

- [bioRxiv API 문서](https://api.biorxiv.org/)
- [PubMed Central 개발자 문서](https://pmc.ncbi.nlm.nih.gov/tools/developers/)
- [NCBI E-utilities 가이드](https://www.ncbi.nlm.nih.gov/books/NBK25501/)
- [Crossref REST API](https://api.crossref.org/)
- [OpenAlex API 문서](https://docs.openalex.org/)

---

## 변경 이력

| 버전 | 일자 | 내용 |
| --- | --- | --- |
| v0.1 | — | 최초 작성 |
| v0.2 | 2026-08-25 | 구조 재편, 기능별 수용 기준·데이터 소스 제약·KPI 정의·일정·미해결 항목 추가 |
| v0.3 | 2026-08-25 | build_plan.md 연결, §13 미해결 항목 중 결정된 사항 반영 |
